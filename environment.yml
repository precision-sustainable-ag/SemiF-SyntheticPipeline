name: synth
channels:
  - conda-forge
  - defaults
dependencies:
  - python==3.11
  - pip
  - hydra-core
  - omegaconf
  # - opencv  # Use 'opencv' instead of 'opencv-python'
  # - pandas
  # - pyyaml
  # - scikit-image
  # - scikit-learn
  # - scipy
  - tqdm
  - albumentations

  # used for graphical analysis
  - matplotlib
  - reportlab
  - pandas
  - seaborn
  - pip:
      # In case any specific packages need to be installed via pip
      # - opencv-python-headless  # Optional: for headless systems without GUI