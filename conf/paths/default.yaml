
workdir: ${hydra:runtime.cwd}  # where the code is executed
projectdir: ${paths.workdir}/projects/${project_name}/${sub_name} # TODO: change this so that projects is it's own directory separate from data
datadir: ${paths.workdir}/data
cutoutdir: ${paths.datadir}/cutouts
backgrounddir: ${paths.datadir}/backgrounds
recipesdir: ${paths.projectdir}/recipes
resultsdir: ${paths.projectdir}/results
species_info: /home/psa_images/SemiF-AnnotationPipeline/data/semifield-utils/species_information/species_info.json

primary_longterm_storage: /mnt/research-projects/s/screberg/longterm_images
secondary_longterm_storage: /mnt/research-projects/s/screberg/longterm_images2
tertiary_longterm_storage: /mnt/research-projects/s/screberg/longterm_images2

# SQL database
sql_database: ${paths.datadir}/db/agir.db