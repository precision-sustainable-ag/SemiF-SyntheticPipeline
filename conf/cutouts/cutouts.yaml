<<<<<<< HEAD
# Configuration file for choosing what type of cutouts you want in your synthetic data set

mode: species # balanced (default), random , batches, species, features, state, mixed
# sampling: stratey for sampling if not enough cutouts exists in specified category
# replace = sampling with replacement
# auto = redistributes cutouts across remaining categories  
# none = without replacement (results in error message)

sampling: # replace, auto, none
save_csv: False # to create and save a csv of all cutout metadata

# balanced: True Will produce the same number of cutouts for each class by copying
balanced: False
sample_size:  # Number of cutouts per class when using balanced or "all"
# Using "all" in sample size gets all available cutouts using the other features. Cannot be used with species proportions
species:   # should only be used when willing to use duplicate cutouts
  # TX
  AMTU: # Waterhemp
  ECCR: # Barnyardgrass
  ECCO2: # Jungle rice
  URTE2: # Texas millet
  BASC5: # Kochia
  HEAN3: # Common sunflower
  PAHY: # Ragweed parthenium
  SOHA: # Johnsongrass
  GLMA4:  # Soybean
  # NC
  AMPA:    # Palmer amaranth
  AMAR2:  100 # Common ragweed
  SEOB4:   # Sicklepod
  XAST:     # Cocklebur
  DISA:     # Large crabgrass
  ELIN3:   # Goosegrass
  URPL2:    # Broadleaf signalgrass
  CYRO:     # Purple nutsedge
  # MD
  AMHY:     # Smooth pigweed
  CHAL7:    # Common lambsquarters
  PADI:     # Fall panicum
  DAST:     # Jimson weed
  ABTH:     # Velvetleaf
  SEPU8:    # Yellow foxtail
  SEFA:     # Giant foxtail
  ERCA20:   # Horseweed
  ZEA:      # Maize

plants_per_pot: 10

max_coty_dist: # for multiple cotys in a single pot
  max: 750 # max min cotys per pot
  min: 5

features:
  dap: 
    min: 30 # atleast days after planting; 
    max: 90 #390
  
  size: # large, medium, small, random (uses bbox area)
  
  is_primary: False
  
  extends_border: False
  
  green_sum: # True or False
    max: 1000000
    min: 5000
  
  area: # mean, 25, 50, 75 for choosing greater than 25%, 50% , 75% percentiles
    max: 
    min: 
 
  area_bbox:
    max:
    min:
    
  area_convex:
    max:
    min:
    
  axis_major_length:
    max:
    min:
    
  axis_minor_length:
    max:
    min:
    
    
  eccentricity:
    max:
    min:
    
    
  solidity:
    max:
    min:
    
  perimeter:
    max:
    min:

state: # percent of species from each state if mode is 'state'
    TX: 30 
    NC: 20
    MD: 50
  
date: # mm-dd-yyyy
=======
filter: True

species:   # USDA symbol from list of species

is_green:
is_primary: True
extends_border: False

green_sum:
  max: 10000000
  min: 800000

area: # mean, 25, 50, 75 for choosing greater than 25%, 50% , 75% percentiles
  max: 
  min: 50

save_csv: True # to create and save a csv of all cutout metadata
>>>>>>> fix-synth
