
count: 4
multiprocess: True
export_yolo_labels: True
pot_jitter: [100, 100]

# savedir: /media/hdd1/mkutu/wir/semifield-synth
# savedir: ${general.workdir}/data/semifield-synth
savedir: ${general.projectdir}/results
datadir: ${synth.workdir}/data
synthdir: ${general.workdir}/data/semifield-synth
potdir: ${synth.synthdir}/pots
backgrounddir: ${synth.synthdir}/backgrounds

# cutout_batchdir: ${data.cutoutdir}/${general.batch_id}
