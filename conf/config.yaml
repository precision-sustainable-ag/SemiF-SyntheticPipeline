defaults:
  - maintenance: maintenance
  - synth: synth
  - _self_

hydra:
  run:
    dir: ./
  output_subdir: 

general:
  batch_id: ??? # Control the batch_id through the command line
  save_to_database: False
  from_database: False
  db: test  # test, trial, WIR
  task: synthesize  # auto_sfm, localize_plants, remap_labels, segment_vegetation
  workdir: ${hydra:runtime.cwd}  # where the code is executed

blob_names:
  synth: semifield-synth
  cutout: semifield-cutouts

data:
  datadir: ${general.workdir}/data
  cutoutdir: /home/admin_mkutugata/SemiF-AnnotationPipeline/data/semifield-cutouts
  batchdir: ${data.cutoutdir}/${general.batch_id} # Is overiden by shell script
  